@using Cv.Models.Entity
@model List<TblSocialMedia>
<div class="social-icons">
    @foreach (var item in Model)
    {
        <a class="social-icon" target="_blank" href="@item.Link"><i class="@item.Class"></i></a>
    }

</div>
